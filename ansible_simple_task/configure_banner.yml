- name: Configure banner on router
  hosts: routers
  gather_facts: no
  tasks:
    - name: Set banner
      cisco.ios.ios_config:
        lines:
          - banner login ^C
          - Welcome to the router! Unauthorized access is prohibited.^C
